Opcional: coloque uma base local de CEP/IBGE (`.xlsx`, `.xls`, `.csv`, `.sqlite` ou `.db`).

Colunas aceitas: `CEP` ou `CEP INICIAL`/`CEP FINAL`, `CIDADE`, `UF`, `IBGE`.
Quando esta base nao existe, a aplicacao tenta consultar a BrasilAPI e salva cache local.
